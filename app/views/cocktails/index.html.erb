<div class="container">

<div class="banner">
<h1> Cocktail mixups! </h1>
</div>

<div class="cards">
<% @cocktails.each do |cocktail| %>


   <div class="card-category">
       <%= link_to cocktail.name.capitalize, cocktail_path(cocktail) %>
  </div>


  <% end %>


<ul class="list-group">
  <% @cocktails.each do |cocktail| %>
    <li class="list-group-item" >
       <%= link_to cocktail.name.capitalize, cocktail_path(cocktail) %>
       </li>
  <% end %>
</ul>

</div>
